{
  "name": "salary-slip-internal-app",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": { "node": ">=22" },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.13.4",
    "prettier": "^3.5.0",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "dev": "pnpm --parallel --stream --filter @salary/api --filter @salary/web dev",
    "build:packages": "pnpm --filter @salary/domain build && pnpm --filter @salary/dingtalk build && pnpm --filter @salary/db build",
    "build": "pnpm build:packages && pnpm --filter @salary/api build && pnpm --filter @salary/web build",
    "test": "pnpm build:packages && pnpm --filter @salary/api build && pnpm -r test",
    "lint": "pnpm -r lint",
    "architecture:check": "node scripts/check-architecture.mjs",
    "typecheck": "pnpm -r typecheck",
    "test:e2e": "playwright test"
  }
}
